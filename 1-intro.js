const amount = 9;
if (amount < 10) {
  console.log("small number");
} else {
  console.log("large number");
}

console.log(`my first node app`);

//anything we would normally log to the browser console we can log to the terminal in the same way by writing it here in javascript and then running:
//node 1-intro.js
//in the command line or terminal (or whatever the file name is)
